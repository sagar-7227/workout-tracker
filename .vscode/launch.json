{
    "version": "0.2.0",
    "configurations": [
        
        
        {
            "name": "Debug Workout Tracker",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/main.go",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}